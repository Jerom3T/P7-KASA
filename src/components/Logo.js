import React from 'react';
import logo from '../images/logo.png';

const Logo = () => {
  return (
    <div className="logo">
      <img src={logo} alt="logo" title="Kasa" />
    </div>
  );
};

export default Logo;
